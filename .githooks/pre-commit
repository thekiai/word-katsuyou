#!/bin/sh

# pre-commit hook: コミット前にビルドをチェック

echo "🔍 コミット前にビルドチェックを実行中..."

# ビルドを実行
npm run build

# ビルドが失敗した場合はコミットを中止
if [ $? -ne 0 ]; then
  echo "❌ ビルドエラーが検出されました。コミットを中止します。"
  echo "エラーを修正してから再度コミットしてください。"
  exit 1
fi

echo "✅ ビルドチェック完了！コミットを続行します。"
exit 0
